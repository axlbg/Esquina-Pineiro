import { HideOnMobileDirective } from './hide-on-mobile.directive';

describe('HideOnMobileDirective', () => {
  it('should create an instance', () => {
    const directive = new HideOnMobileDirective();
    expect(directive).toBeTruthy();
  });
});
