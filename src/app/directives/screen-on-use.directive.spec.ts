import { ScreenOnUseDirective } from './screen-on-use.directive';

describe('ScreenOnUseDirective', () => {
  it('should create an instance', () => {
    const directive = new ScreenOnUseDirective();
    expect(directive).toBeTruthy();
  });
});
