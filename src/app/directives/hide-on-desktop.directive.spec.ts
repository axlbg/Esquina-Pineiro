import { HideOnDesktopDirective } from './hide-on-desktop.directive';

describe('HideOnDesktopDirective', () => {
  it('should create an instance', () => {
    const directive = new HideOnDesktopDirective();
    expect(directive).toBeTruthy();
  });
});
